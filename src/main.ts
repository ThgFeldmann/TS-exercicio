
function multiplicação(a: number, b: number) { // função multiplicativa
    return a * b;
}

console.log(multiplicação(10, 4)); // declarando os valores para a multiplicação

function saudação(nome: string) { // função de saudação
    return "Olá " + nome + "!";
}

console.log(saudação('Fulano')); // declarando o valor para a saudação